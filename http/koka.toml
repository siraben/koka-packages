[package]
name = "http"
version = "0.1.0"
koka = ">=3.2,<4"

[sources]
directories = ["src"]

[dependencies]
bytes = { path = "../bytes" }
strbuilder = { path = "../strbuilder" }
runtime = { path = "../runtime" }
logging = { path = "../logging" }
kktest = { path = "../kktest" }

[native]
pkg-config = ["libuv"]

[targets.test]
directories = ["test"]

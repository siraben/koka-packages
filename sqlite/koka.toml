[package]
name = "sqlite"
version = "0.1.0"
koka = ">=3.2,<4"

[sources]
directories = ["src"]

[dependencies]
bytes = { path = "../bytes" }
resource = { path = "../resource" }
kktest = { path = "../kktest" }

[native]
pkg-config = ["sqlite3"]

[targets.test]
directories = ["test"]
